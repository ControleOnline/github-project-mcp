name: "[Disabled] Agent Flow Sync"

on:
  workflow_dispatch:

jobs:
  agent-flow-sync:
    if: ${{ false }}
    runs-on: ubuntu-latest

    steps:
      - run: echo "Disabled. Agent flow sync now belongs to peer agents in ChatGPT."