name: "[Disabled] CTO Project Supervisor"

on:
  workflow_dispatch:

jobs:
  cto-runner:
    if: ${{ false }}
    runs-on: ubuntu-latest

    steps:
      - run: echo "Disabled. CTO supervision now belongs to peer agents in ChatGPT."