name: "[Disabled] Developer Project Runner"

on:
  workflow_dispatch:

jobs:
  developer-runner:
    if: ${{ false }}
    runs-on: ubuntu-latest

    steps:
      - run: echo "Disabled. Developer execution now belongs to peer agents in ChatGPT."