name: "[Disabled] DevOps Project Runner"

on:
  workflow_dispatch:

jobs:
  devops-runner:
    if: ${{ false }}
    runs-on: ubuntu-latest

    steps:
      - run: echo "Disabled. DevOps execution now belongs to peer agents in ChatGPT."