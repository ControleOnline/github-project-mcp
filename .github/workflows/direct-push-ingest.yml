name: "[Disabled] Direct Push Ingest"

on:
  workflow_dispatch:

jobs:
  ingest:
    if: ${{ false }}
    runs-on: ubuntu-latest

    steps:
      - run: echo "Disabled. Direct push ingestion now belongs to peer agents in ChatGPT."