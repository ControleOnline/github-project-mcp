name: "[Disabled] QA Project Runner"

on:
  workflow_dispatch:

jobs:
  qa-runner:
    if: ${{ false }}
    runs-on: ubuntu-latest

    steps:
      - run: echo "Disabled. QA execution now belongs to peer agents in ChatGPT."