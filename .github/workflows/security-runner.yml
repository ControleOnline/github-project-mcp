name: "[Disabled] Security Project Runner"

on:
  workflow_dispatch:

jobs:
  security-runner:
    if: ${{ false }}
    runs-on: ubuntu-latest

    steps:
      - run: echo "Disabled. Security execution now belongs to peer agents in ChatGPT."