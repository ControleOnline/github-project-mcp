# Developer

Entrada operacional do agent `Developer` dentro de `automate/agents/`.

## Arquivos

- `dispatch.mjs`: caminho preferencial para o runner operacional do `Developer`

## Observação

A implementação compartilhada do despacho continua reaproveitando `automate/scripts/agent-project-dispatch.mjs`, mas o ownership operacional do agent agora começa explicitamente nesta pasta.
